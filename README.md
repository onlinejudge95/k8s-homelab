# Kubernetes Homelab

This repository contains the infrastructure and application configuration for my local Kubernetes cluster.

## Structure

- `infrastructure/`: Code to provision the cluster (Kind, Vagrant, etc.).
- `apps/`: Helm charts and configuration for applications.
- `scripts/`: various helper scripts for automation.
